var React = require('react');

var Countdown = React.createClass({
  render: function(props){
    return(
      <div>
        <h1>Countdown</h1>
      </div>

    );
  }
});

module.exports = Countdown;
